spring:

  application:
    name: kiosc-api

  datasource:
    url: jdbc:mysql://localhost:3306/kiosco_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  secret: miClaveSecretaSuperLargaParaJWTqueDebeSerDe256bits
  expiration: 86400000